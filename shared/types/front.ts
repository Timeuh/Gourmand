// month variety progress bar stop
export type ProgressBarStop = {
  stop: number;
  justify: string;
};

// a calendar day entry
export type CalendarDay = {
  day: number;
  month: number;
  year: number;
};
