info:
  name: update preptime
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/preptimes/1
  body:
    type: json
    data: |-
      {
        "time": "15min"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
