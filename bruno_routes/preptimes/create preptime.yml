info:
  name: create preptime
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/preptimes
  body:
    type: json
    data: |-
      {
        "time": "15min"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
