info:
  name: update ingredient
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/ingredients/1
  body:
    type: json
    data: |-
      {
        "name": "salt"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
