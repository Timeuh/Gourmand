info:
  name: all foods
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/foods
  params:
    - name: fullContent
      value: "true"
      type: query
      disabled: true
    - name: userId
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
