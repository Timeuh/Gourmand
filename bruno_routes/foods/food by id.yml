info:
  name: food by id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/foods/1?fullContent=true
  params:
    - name: fullContent
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
