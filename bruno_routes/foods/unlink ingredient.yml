info:
  name: unlink ingredient
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:3000/api/foods/1/ingredients/1
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
