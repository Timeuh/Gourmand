info:
  name: update calendar
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/calendars/1
  body:
    type: json
    data: |-
      {
        "date": "2026-06-28T12:00:00Z",
        "food_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
