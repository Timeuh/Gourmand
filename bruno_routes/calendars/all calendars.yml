info:
  name: all calendars
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/calendars?fullContent=true&userId=1
  params:
    - name: fullContent
      value: "true"
      type: query
    - name: userId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
