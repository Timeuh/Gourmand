info:
  name: calendar by id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/calendars/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
