info:
  name: create color
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/colors
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  body:
    type: json
    data: |-
      {
        "code": "#FFFFFF"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
