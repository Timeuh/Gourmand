info:
  name: update color
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/colors/1
  body:
    type: json
    data: |-
      {
        "code": "#FFFFFF"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
