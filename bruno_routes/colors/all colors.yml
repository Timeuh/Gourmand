info:
  name: all colors
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/colors
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
