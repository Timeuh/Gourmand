info:
  name: update theme
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/themes/1
  body:
    type: json
    data: |-
      {
        "name": "Theme"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
