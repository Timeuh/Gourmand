info:
  name: link a color
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/themes/1/colors/1
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
