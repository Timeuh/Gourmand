info:
  name: theme by id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/themes/1?fullContent=true
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  params:
    - name: fullContent
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
