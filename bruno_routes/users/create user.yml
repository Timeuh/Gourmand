info:
  name: create user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "email": "john.doe@mail.com",
        "theme_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
