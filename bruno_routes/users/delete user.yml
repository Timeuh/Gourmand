info:
  name: delete user
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/users/1
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
