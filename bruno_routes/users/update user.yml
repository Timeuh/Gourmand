info:
  name: update user
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/users/1
  body:
    type: json
    data: |-
      {
        "email": "john.doe@mail.com",
        "theme_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
