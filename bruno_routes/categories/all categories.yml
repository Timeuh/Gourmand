info:
  name: all categories
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/categories
  headers:
    - name: Authorization
      value: Bearer {{NUXT_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
