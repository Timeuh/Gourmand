info:
  name: create plate
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/plates
  body:
    type: json
    data: |-
      {
        "number": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
