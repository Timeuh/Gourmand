info:
  name: update plate
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/plates/1
  body:
    type: json
    data: |-
      {
        "number": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
